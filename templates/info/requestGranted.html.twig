{% extends 'formWrapper.html.twig' %}

{% block form %}
    <h2 class="mb-5">Votre requête a été acceptée</h2>
    {{ message }}
    <a href="{{ path('home') }}" class="btn btn-primary">Retourner à la page d'accueil</a>
{% endblock %}