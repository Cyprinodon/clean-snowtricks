{% extends 'formWrapper.html.twig' %}

{% block form %}
    <h2>Réninitialiser son mot de passe</h2>

    {{ form_start(form) }}
    {{ form_row(form.username) }}

    <button type="submit" class="btn btn-primary">Faire une demande</button>
    {{ form_end(form) }}
{% endblock %}